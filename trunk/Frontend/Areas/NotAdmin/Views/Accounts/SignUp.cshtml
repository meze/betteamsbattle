@using BetTeamsBattle.Frontend.Areas.NotAdmin.Models.Accounts
@using Resources
@model SignUpViewModel
@{
    Layout = MVC.Shared.Views.OneColumn._OneColumnLayout;
}
@section Title{
    @Accounts.SigningUp
}
<div class="text_login">
    @Accounts.OpenIdUrlFirstTime
</div>
@using (Html.BeginForm(MVC.NotAdmin.Accounts.SignUp(), FormMethod.Post, new { @class="form" }))
{
    <text>
    @Html.ValidationSummary(true, Common.ValidationSummaryCaption)
    @Forms.TextBox(Html, m => m.Login, Accounts.Login)
    @Html.HiddenFor(m => m.OpenIdUrl)
    @Forms.SubmitButton(Accounts.SignUp)
    </text>
}