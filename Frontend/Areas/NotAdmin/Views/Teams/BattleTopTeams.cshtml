@using BetTeamsBattle.Frontend.Areas.NotAdmin.Models.Teams
@using BetTeamsBattle.Frontend.Models.Shared
@using Resources
@model IEnumerable<TopTeamViewModel>

@Html.Partial(MVC.Shared.Views.TwoColumns._LeftColumnBlock, new ColumnBlockViewModel(Battles.BattleLeaders, @<text>
@foreach (var team in Model)
{
    <div class="list">
        @Html.Partial(MVC.NotAdmin.Teams.Views._Team, team)
    </div>
}
</text>))