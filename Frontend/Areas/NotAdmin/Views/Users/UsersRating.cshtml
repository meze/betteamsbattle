@using BetTeamsBattle.Frontend.Areas.NotAdmin.Models.Users
@using BetTeamsBattle.Frontend.Models.Shared
@using Resources
@model IEnumerable<UsersRatingUserViewModel>
@Html.Partial(MVC.Shared.Views._LeftColumnBlock, new LeftColumnBlockViewModel(Users.UsersRating, @<text>
@foreach (var user in Model)
{
    <div class="list">
        @user.Login @user.Rating
    </div>
}
</text>))
